import { EmailsModule } from '../emails/emails.module';
import { GeoLocationsModule } from '../geo-locations/geo-locations.module';

const modules = [GeoLocationsModule, EmailsModule];

export default modules;
